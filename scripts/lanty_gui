#!/usr/bin/env bash
# Launcher for the Lanty chat GUI.
set -e
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
cd "$PROJECT_ROOT"
# shellcheck source=/dev/null
source inference/.venv/bin/activate
exec python -m gui.main "$@"
